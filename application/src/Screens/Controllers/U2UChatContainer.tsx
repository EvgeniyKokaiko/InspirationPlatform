import React, {useState} from 'react';
import ChatComponent from "../ChatComponent";

type IProps = {}
type IState = {}

const U2UChatContainer = () => {
    const [getState, setState] = useState<IState>({
        
    })

    const STATE = {

    }

    return <ChatComponent {...STATE} />

};

export default U2UChatContainer;